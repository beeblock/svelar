export const databaseSeeder = `import { Seeder } from 'svelar/database';

export class DatabaseSeeder extends Seeder {
  async run(): Promise<void> {
    // Call other seeders here
    // await this.call(UsersSeeder);
  }
}
`;
